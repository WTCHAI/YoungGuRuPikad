fn main(position_x: Field, position_y: Field, target_x: Field, target_y: Field, radius: pub Field) {

}

fn YoungGuRuPikadMai (position_x: Field, position_y: Field, target_x: Field, target_y: Field, radius: Field){ 
    
}

#[test]
fn test_main() {
    // main(1, 2);

    // Uncomment to make test fail
    // main(1, 1);
}
